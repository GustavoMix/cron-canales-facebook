name: Probar Facebook Live Bolivia - Grupo 2

on:
  workflow_dispatch:
    inputs:
      forzar:
        description: "Ignorar cooldown/cache y revisar aunque se haya revisado hace poco"
        type: boolean
        required: false
        default: false
  schedule:
    # Fuentes 22-42 de fuentes.json. Los 4 grupos corren cada media hora, en paralelo.
    - cron: "0,30 * * * *"

jobs:
  escanear:
    permissions:
      contents: write
    uses: ./.github/workflows/_probar-lives-grupo.yml
    with:
      grupo: grupo2
      offset: 21
      limit: 21
      forzar: ${{ inputs.forzar == true }}
